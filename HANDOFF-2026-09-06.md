# 交接文档（2026-09-06）

仓库 `shuakami/timetable`，`main` 已推到 `5d4c780`（全部改动直接进主分支，未开 PR）。
上一份交接 `HANDOFF-edu-import.md`（教务导入方案）继续有效，本文只写增量。

## 0. 会话约定（照旧）

- 会话开头向用户申请 ghp token（临时、仅本会话、不保存）。
- 推送只用 `scripts/push-github.sh`（`GH_TOKEN=... scripts/push-github.sh timetable "msg"`），author 固定 `shuakami <shuakami@sdjz.wiki>`，禁止 gh CLI。脚本会 `git add -A`，别把 `android/local.properties`、token 放进工作区。
- 不跑安卓、不开模拟器、不 `adb install`，最多浏览器截图。验证只做 `npx tsc --noEmit && npx vitest run && npm run build`（Node 22，`cap sync` 需要 Node ≥22）。
- 原型 `src/App.tsx` 是 UI 事实来源，生产壳逐屏照搬。全屏内页、底部 `PrimaryButton`、无 stagger/弹簧。

当前状态：tsc 通过，vitest 11 文件 110 用例全过，build 通过，`npx cap sync android` 通过。Gradle 没跑（环境只有 JDK 17）。

## 1. 已完成

| 项 | 位置 | 说明 |
| --- | --- | --- |
| 复制 Prompt / 粘贴 JSON 真可用 | `src/app/widgets.ts` `copyText/pasteText`，`WidgetBridge.java` | 原生剪贴板桥 + 浏览器 `navigator.clipboard`/`execCommand` 回退 |
| 相册权限 | `TtCamera.java`，`src/app/camera.ts` | 根因：Android 13+ 一次请求里混了被 maxSdk 裁掉的旧权限导致整体 reject。已按 API 分组申请；拒绝后引导到系统设置（`openAppSettings`） |
| 作息时间独立页 | `src/app/schedule.tsx`，`src/domain/schedule.ts` | 从学期页拆出。总节数（4–20）、标准时长（20–120，步 5）两个步进器；只填每节开始，下课自动 = 开始 + 时长；单节可改下课并标「已改」、可恢复自动；重叠/非法校验。`generateGrid` 渐进式午休/晚间布局，14/16 节放不下时逐级放宽 |
| 引导 | `src/app/Onboarding.tsx` | 顺序：介绍 → 开学日期 → 作息时间 → 课表来源 |
| 滚轮阻尼 + 震动 | `src/app/ui.tsx`（`WHEEL_*` 常量），`haptic(kind)` | 指针手势实现，速度上限、ease-out 对齐、越界 rubber-band；刻度 `tick`、边界 `edge`、确认 `select`、`press`、`reject` 五种触感 |
| 节次来源优先级 | `src/domain/importer.ts` `parseTimeSlots`，`src/app/RealApp.tsx` `ImportRunPage`，`src/app/semester.ts` `isDefaultGrid/extendGrid` | JSON/AI 输出可带 `timeSlots`。解析阶段不写 Store；预览页出现「采用文件里的作息时间」开关（当前作息仍是出厂默认、或文件带学期元数据时默认开；用户改过作息则默认关）；课程节次超出网格自动追加、不改已有节次；确认时一次性保存学期+课程。AI Prompt 已要求有节次时间就返回 `timeSlots` |
| 周视图卡片动态高度 | `src/domain/time-axis.ts`，`src/app/week-axis.tsx` | 纵轴改为「按节次分段」：每节固定 `AXIS_ROW=56`，短课间 8，长间隔（午休/晚间）画成带时长标签的分隔带；卡片按真实高度选内容（`cardFit`）：≥52 两行名+地点、≥38 一行名+地点、≥26 一行名、≥17 密排一行、更低只留色块；冲突并排窄卡竖排多行且不显示半截字 |
| 教务导入地基（未接 UI） | 见 §3 | 原生内置浏览器插件、指纹、正方新版解析、学校索引、注入脚本协议 + 单测 |

截图（`/home/ubuntu/shots/out/`，机器本地，可能已丢）：`prod-week*.png`、`prod-schedule*.png`、`prod-import-grid*.png`、`onboard-schedule*.png`。

## 2. 用户最新反馈（必须处理，最高优先级）

周视图（`src/app/week-axis.tsx` + `src/domain/time-axis.ts`）：

1. **午休/晚间分隔带**：用户认可这个想法，但它和普通格子/表格线放在一起视觉不协调——"又有表格，又有那个"，看着乱。要重新设计分隔带与网格线的关系：要么分隔带取代那一段的网格线（带内不画横线、竖线淡出），要么把分隔带做成和行同一套语言（同样的行高节律、只是底色/标签不同），不要两套视觉系统叠加。
2. **课程卡片过高、挤在一起**：截图里「线性代数」「马克思主义…」两张卡 H 很高，且相邻挤成一团，视觉很差。要查原因：疑点 (a) 连堂课（如 1–4 节）跨越了短课间，卡片高度 = 多行 + 课间，与旁边单节卡对比失衡；(b) 相邻卡片之间没有留 gap（卡片上下边紧贴）；(c) 冲突并排时列宽太窄导致高度显得更夸张。修法方向：卡片上下各留 2–3px 间隙、跨课间的卡在课间处不填满（或用更浅的底色表示课间）、并排卡片统一宽度和圆角、高卡片里的文字仍顶部对齐不居中拉伸。改完出 `prod-week.png` 浅/深截图给用户确认。

## 3. 教务导入：已做 / 未做

已做（全部有单测 `src/domain/__tests__/edu.test.ts`）：

- `android/.../TtEdu.java`（已在 `MainActivity` 注册；Manifest 加了 `usesCleartextTraffic=true`，教务站多为 http）
  - 独立 WebView 放在应用 WebView **下面**，应用 WebView 透明（MIUI/华为留 1/255 alpha，做法同 capacitor-inappbrowser）；页面中间留透明区 `frame({top,bottom,keep:[rect],interactive})`（CSS px），落在透明区且不在 `keep` 里的触摸整段转发给下面的 WebView。
  - 方法：`open({url})`、`close()`、`navigate({url})`、`reload()`、`back()→{went}`、`frame(...)`、`eval({js})→{value}`、`state()`。事件：`nav {url,title,loading,progress,canGoBack,error?}`、`message {data}`（页面里 `TtBridge.post(str)`）。
  - 会话隔离：open/close 都清 Cookie，close 时清该站 localStorage/sessionStorage + 缓存并 destroy。SSL 错误一律 cancel 并上报 `error:'ssl'`（可能挡住证书过期的学校，需要用户决定是否放开）。
  - **未编译过**（无 JDK 21/SDK）。需 `./gradlew assembleDebug` 验证；要重点看 `bridge.getWebView().getParent()` 加子视图与 `setOnTouchListener` 转发。
- `src/domain/edu/systems.ts`：系统 id/中文名、URL 指纹 `detectSystem`、`isTimetablePage`、`hostOf`、`scrubUrl`（反馈用去 query）。
- `src/domain/edu/schools.ts` + `schools.json`：1542 所学校 `[name, system, url]`（来自 baoozak/timetable，MIT，需在 README/关于页署名）。`searchSchools`、`urlFromQuery`、`schoolByUrl`。
- `src/domain/edu/zhengfang.ts`：正方新版 `kbList` → `RuleOutput`（`zcdToWeeks` 逐段单双周、`parseJcs` 支持 `3-4`/`0304`/`5`、坏行进诊断、不带作息）、`termLabel`、`guessTerm`。
- `src/domain/edu/scripts.ts`：注入脚本。`PROBE_JS`（页面是否有课表表格、正方 `#xnm/#xqm` 下拉）、`zfFetchJs(xnm,xqm)`（同源 POST `kbcx/xskbcx_cxXsgrkb.html?gnmkdm=N253508`，只回传 kcmc/xm/cdmc/xqj/jcs/zcd）、`PAGE_HTML_JS`、`PAGE_TEXT_JS`、`wrapRun(id, body)`（结果 `{id,ok,r|e}` 经 `TtBridge.post` 回传）、`zfTermOptions`。
- `tsconfig.json` 加了 `resolveJsonModule`。

未做（按顺序）：

1. `src/app/edu-browser.ts`：`registerPlugin<TtEduPlugin>('TtEdu')` 包装；`run(js)`：生成 id → `eval(wrapRun(id, body))` → 等 `message` 事件里同 id 的结果，20s 超时；浏览器环境全部 no-op/reject（生产页在浏览器里只能截图）。
2. `src/app/edu.tsx` 生产 7 屏（原型已有：`EduEntryScreen`、`EduBrowserScreen`、`EduTermSheet`、`EduPreviewScreen`、`EduFailScreen`，文案见原型）：
   - 选择学校：搜索框 placeholder「学校名或教务网址」，最近使用（localStorage `tt.edu.recent`），结果行 = 校名 + 系统 + 箭头；输入网址直接进浏览器。
   - 内置浏览器页：顶部返回圆钮 / 主机名胶囊（单行截断）/ 刷新圆钮；中间是透明洞；底部悬浮胶囊用 `dockStyle`（禁止黑底）：未识别「登录后打开课表页」（灰），识别出课表页「导入 N 门课」（主题色）。`nav` 事件 → `detectSystem` + `isTimetablePage` → 课表页时跑 `PROBE_JS`（正方再跑当前学期 `zfFetchJs` 拿数量）。
   - 透明实现：Page 挂载完成（推入动画结束）后 `TtEdu.open` + 在 `<html>` 加 `tt-edu` class；`src/index.css` 加：`html.tt-edu, html.tt-edu body, html.tt-edu #root, html.tt-edu #root>div, html.tt-edu [data-shell] {background:transparent!important}`、`html.tt-edu [data-shell]>*:not([data-edu-keep]), html.tt-edu #root>div>*:not([data-shell]) {visibility:hidden}`、`html.tt-edu [data-edu-keep] [data-veil-host]{background:transparent!important}`；`Page` 组件加一个 prop 在外层 motion.div 上输出 `data-edu-keep`。用 `ResizeObserver` 量洞和胶囊 rect 调 `frame()`；抽屉打开时 `interactive:false`。离开时先 `close()` 再 pop。
   - 系统返回键：`canGoBack` 则 `back()`，否则出栈。
   - 选学期 Sheet「导入哪个学期？」：`zfTermOptions` + `termLabel`，默认选中页面当前学期；只有正方才有，其他系统直接进预览。
   - 预览：复用 `ImportRunPage`——给它加 `initialOut?: RuleOutput` 跳过输入阶段，标题「N 门课」，加原型里的周网格缩略图；规则对象本地构造 `{id:'builtin-edu', name:'教务系统', input:'json', ...}` 不要加进 `BUILTIN_RULES`（否则会出现在规则列表里）。确认走现有 `store.setSemester + applyImport`。
   - 未识别页「没有识别到课表」：显示脱敏地址；三项：「让 AI 转换」（跑 `PAGE_TEXT_JS`，把页面文字带进 `AiImportPage`，复制时 Prompt + 文字一起）、「更新到最新版本」（GitHub Releases latest 与 `CapApp.getInfo().version` 比较，新才显示）、「反馈这个页面」（打开 GitHub new issue，正文只带 `scrubUrl`、系统猜测、版本号）；底部「返回」。
   - 非正方系统的兜底：`PAGE_HTML_JS` → 现有 `parseHtml(html, {mode:'grid'})`（`src/domain/importers/html.ts`），能解出就进预览，否则进未识别页。
3. 路由：`RealApp.tsx` `Route` 加 `eduSchool` / `eduBrowser` / `eduFail`；`ImportPage` 顶部加「从教务系统导入」；`Onboarding.tsx` `SOURCES` 里 `['builtin-html','教务系统']` 改成走教务入口。
4. 其余系统专用脚本（可直接从 `/tmp/timetable-main/utils/parsers/*.js`、即 baoozak 仓库移植，质量参差；先靠通用表格兜底）；每个系统一份 fixture + 单测。
5. 拾光脚本接入 + 失败反馈（见上一份交接）。
6. `AGENTS.md` 的「安全」段要改成新边界：不收集账号密码、不后台登录抓取；内置浏览器由用户自己登录、只在用户点导入时读当前页/同源接口、先预览再应用、会话 Cookie 独立并在离开时清理、不下载执行未校验的远程脚本。
7. 最后：`tsc/vitest/build`，有 JDK 21 + SDK 时 `cap sync` + `gradlew assembleDebug`；按用户指示推送。

## 4. 已知问题 / 注意

- ESLint 不可用（无 eslint 9 配置），别去加。
- Capacitor CLI 需要 Node ≥22：`nvm use 22`。
- 原生代码（TtEdu、TtCamera 权限分组）都没编译过，第一个有 JDK 21 的会话先 `assembleDebug`。
- `usesCleartextTraffic=true` 是全局的，对应用自身请求也生效，用户可接受与否未确认。
- `SYSTEM_LABEL` 里 baoozak 的 `kingosoft_new` 标成青果、`wisedu` 标成金智，命名沿用其数据，可能不准。
- 导入预览默认采不采用文件作息的规则（§1 表）是我定的，用户没有逐条确认。
- 教务导入的"进入课表页就预读数量"是为了胶囊显示「导入 N 门课」（原型如此），严格来说在点导入前读了同源接口；如用户不接受，改成点了再读、胶囊只写「导入课表」。
